{
  "id": "stablepay-mock-plugin",
  "name": "StablePay Local Wallet Plugin",
  "description": "OpenClaw plugin for StablePay local wallet runtime, DID registration, mock X verification, and local signing flows.",
  "version": "0.2.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "backendBaseUrl": {
        "type": "string",
        "format": "uri",
        "description": "StablePay API Gateway base URL, for example http://127.0.0.1:8080"
      },
      "verifyPageBaseUrl": {
        "type": "string",
        "format": "uri",
        "description": "Verification page base URL, for example http://127.0.0.1:3000/verify"
      },
      "requestTimeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 30000,
        "default": 8000,
        "description": "HTTP request timeout in milliseconds"
      },
      "rewardAmount": {
        "type": "number",
        "minimum": 0,
        "default": 1,
        "description": "Displayed reward amount for the mock verification flow"
      },
      "localStatePath": {
        "type": "string",
        "description": "Path to the encrypted local plugin state file"
      },
      "localStateKeyEnv": {
        "type": "string",
        "description": "Environment variable that provides the master key used to encrypt the local plugin state"
      },
      "owsVaultPath": {
        "type": "string",
        "description": "Optional OWS vault path override when a supported OWS runtime is available"
      },
      "owsPassphraseEnv": {
        "type": "string",
        "description": "Environment variable that stores the OWS passphrase"
      },
      "owsRuntime": {
        "type": "string",
        "enum": ["auto", "ows-sdk", "ows-cli", "wsl-ows", "local-dev"],
        "default": "auto",
        "description": "Preferred local signing runtime"
      },
      "walletNamePrefix": {
        "type": "string",
        "description": "Wallet name prefix used when creating local wallets"
      },
      "didRegisterPath": {
        "type": "string",
        "description": "API path used to register a client-side wallet into DID Service"
      },
      "allowLegacyDidCreateFallback": {
        "type": "boolean",
        "default": false,
        "description": "Whether to fall back to the old /api/v1/did endpoint when /api/v1/did/register is not available"
      }
    },
    "required": ["backendBaseUrl"]
  },
  "uiHints": {
    "backendBaseUrl": {
      "label": "StablePay gateway URL",
      "help": "Base URL for api-gateway, for example http://127.0.0.1:8080",
      "placeholder": "http://127.0.0.1:8080"
    },
    "verifyPageBaseUrl": {
      "label": "Verify page URL",
      "help": "Optional. Used when generating a shareable verify link",
      "placeholder": "http://127.0.0.1:3000/verify"
    },
    "localStatePath": {
      "label": "Local state path",
      "help": "Encrypted local state file used by the plugin runtime"
    },
    "localStateKeyEnv": {
      "label": "State key env",
      "help": "Environment variable used as the master key for local AES-256-GCM encryption"
    },
    "owsRuntime": {
      "label": "Signing runtime",
      "help": "Prefer auto. The plugin will use OWS when available, otherwise local-dev on Windows"
    },
    "didRegisterPath": {
      "label": "DID register path",
      "help": "Target API path for client-side DID registration"
    }
  }
}
