{
  "name": "@stablepay/openclaw-stablepay-plugin",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "StablePay OpenClaw plugin for local wallet runtime, DID registration, mock X verification, and local signing.",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@open-wallet-standard/core": "^1.1.2",
    "@sinclair/typebox": "^0.34.41"
  },
  "peerDependencies": {
    "openclaw": ">=2026.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
